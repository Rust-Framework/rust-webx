---
description: Docbit IRequestHandler 编写规范（DbContext 读写、格式与空行）
globs: docbit/handlers/**/*.rs
alwaysApply: false
---

# Docbit Handler 规范

## 软删除

全局查询过滤器在 `docbit_domain::prepare_context` 注册，**禁止**在 handler 的 `linq!` 中重复 `!is_deleted`。

## 审计字段

`ToEntity` / `ApplyTo` 在 domain mapper 内通过 `RequestContext::operator_id()` 填充 `created_id` / `updated_id`。Handler 只需调用 `req.to_entity(id, now)` / `req.apply_to(&mut entity, now)`，软删除时 `entity.updated_id = crate::util::operator_id()`。

## 持久化

```rust
use crate::db::{save_changes, EfResultExt};

save_changes(&mut self.ctx).await?;
let rows = linq!(self.ctx.set::<User>(), |u: User| u.email == q)
    .to_list()
    .await
    .map_ef()?;
```

## 写后 reload（含导航）

返回 DTO 需要 `include` 导航字段时，save 后用 `ef_require_by_id!` 重载：

```rust
save_changes(&mut self.ctx).await?;

let saved = crate::ef_require_by_id!(
    self.ctx,
    Blog,
    id,
    Error::NotFound("Blog not found after save".into());
    include row.category;
    include row.author
);
Ok(saved.to_model())
```

无导航的简单实体可直接用 tracked entity 转 model。

## 写操作步骤

`set` → `add`/`update` → 一次 `save_changes`；禁止链式 `self.ctx.set::<T>().add(...)`。
