# Generated by Cargo
# will have compiled files and executables
target/
output/
.tools/
publish/
# Cargo.lock is NOT ignored because this workspace contains binary crates
# (the lrwf-demo crate). See:
# https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html

# These are backup files generated by rustfmt
**/*.rs.bk

# Local runtime SQLite databases
*.db
*.db-shm
*.db-wal

# IDE / AI agent helpers (do not publish)
.trae/
.agents/
.idea/
.vscode/
*.swp
*.swo

# Sibling ecosystem doc mirrors (copied by scripts/sync-docs.* for publish bundles).
# Only docs/rust-webx/ is committed — canonical sources live in sibling repos.
docs/rust-dix/
docs/rust-ef/
docs/rust-agent-framework/
docs/rust-gpui-rml/
